version: '3.8'




































































































        run: docker build -t scl-sms-platform:${{ github.sha }} .      - name: Build Docker image      - uses: actions/checkout@v4    steps:    needs: lint-and-typecheck    runs-on: ubuntu-latest  build:          NODE_ENV: test          JWT_SECRET: test-secret          REDIS_URL: redis://localhost:6379          DATABASE_URL: postgresql://scl:scl_secret_2024@localhost:5432/scl_sms_test        env:        run: cd server && npm test      - name: Run tests          DATABASE_URL: postgresql://scl:scl_secret_2024@localhost:5432/scl_sms_test        env:        run: cd server && npx prisma migrate deploy --schema=../prisma/schema.prisma      - name: Run migrations        run: cd server && npx prisma generate --schema=../prisma/schema.prisma      - name: Generate Prisma client        run: cd server && npm ci      - name: Install dependencies          cache-dependency-path: server/package-lock.json          cache: 'npm'          node-version: '20'        with:      - uses: actions/setup-node@v4      - uses: actions/checkout@v4    steps:          --health-retries 5          --health-timeout 5s          --health-interval 10s          --health-cmd "redis-cli ping"        options: >-          - 6379:6379        ports:        image: redis:7-alpine      redis:          --health-retries 5          --health-timeout 5s          --health-interval 10s          --health-cmd "pg_isready -U scl"        options: >-          - 5432:5432        ports:          POSTGRES_DB: scl_sms_test          POSTGRES_PASSWORD: scl_secret_2024          POSTGRES_USER: scl        env:        image: postgres:16-alpine      postgres:    services:    needs: lint-and-typecheck    runs-on: ubuntu-latest  test:        run: cd client && npx tsc --noEmit      - name: TypeScript check (client)        run: cd server && npx tsc --noEmit      - name: TypeScript check (server)        run: cd server && npx prisma generate --schema=../prisma/schema.prisma      - name: Generate Prisma client        run: cd client && npm ci      - name: Install client dependencies        run: cd server && npm ci      - name: Install server dependencies            client/package-lock.json            server/package-lock.json          cache-dependency-path: |          cache: 'npm'          node-version: '20'        with:      - uses: actions/setup-node@v4      - uses: actions/checkout@v4    steps:    runs-on: ubuntu-latest  lint-and-typecheck:jobs:    branches: [main]  pull_request:    branches: [main, develop]  push:on:services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scl-app
    restart: unless-stopped
    ports:
      - '3001:3001'
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://scl:scl_secret_2024@postgres:5432/scl_sms
      REDIS_URL: redis://redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  nginx:
    image: nginx:alpine
    container_name: scl-nginx
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app

  postgres:
    image: postgres:16-alpine
    container_name: scl-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: scl
      POSTGRES_PASSWORD: scl_secret_2024
      POSTGRES_DB: scl_sms
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U scl -d scl_sms']
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: scl-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
