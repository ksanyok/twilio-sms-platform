feat: comprehensive audit fixes — security, validation, performance, UX

TIER 1 (Critical/High):
- Fix authStore deadlock: add initialized=true on 401/403 catch
- Zod env validation: fail-fast startup if missing DATABASE_URL/JWT_SECRET
- Zod API validation on ALL 23 endpoints (schemas + middleware)
- Socket.IO conversation authorization (role-based access)
- Role restriction on analytics + diagnostics (ADMIN/MANAGER only)
- Cascade deletes: Lead->Conversations->Messages, Assignments, etc.
- 8 new DB indexes: Message.phoneNumberId/sentByUserId, Lead.source, etc.
- Remove redundant indexes (DailyNumberStats, SuppressionEntry)
- Campaign auto-COMPLETED: detect when all messages processed

TIER 2 (Quality):
- Redis user cache in auth middleware (60s TTL, reduces DB hits)
- Distributed lock for automation worker (Redis SETNX)
- Streaming CSV export (cursor-based, OOM-safe for large datasets)
- Twilio diagnostics endpoint: balance, account status, A2P brands,
  campaigns, messaging services, phone numbers, usage, compliance
- Debounce search inputs (300ms) on Leads, Inbox, Campaigns pages
- SMS character counter component (GSM-7/UCS-2, segment count)
- Confirmation dialog for tag deletion in Settings

TIER 3 (Polish):
- Soft-delete for leads (deletedAt field, filters in queries)
- Light theme improvements: modal backdrops, hover states, focus rings
- ARIA labels on reply textarea
- ConfirmDialog reusable component
- useDebounce custom hook

Files: 30 changed (5 new, 25 modified)
Build: Backend tsc clean, Frontend vite build clean
